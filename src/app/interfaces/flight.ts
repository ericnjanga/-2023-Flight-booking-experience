export interface Flight {
    id: number;
    departureCity: string;
    departureDate: string;
    arrivalCity: string;
    arrivalDate: string;
    price: number;
    airline: string;
    duration: string;
}